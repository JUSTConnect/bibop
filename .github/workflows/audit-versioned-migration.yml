name: Retire Versioned Migration Audit

on:
  pull_request:

permissions:
  contents: write

jobs:
  retire:
    if: github.event.pull_request.number == 1202
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Remove retired audit
        run: |
          rm -f .github/workflows/audit-versioned-migration.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -u .github/workflows/audit-versioned-migration.yml
          git commit -m "Remove retired versioned migration audit"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
